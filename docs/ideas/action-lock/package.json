{
  "name": "action-lock-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "User-owned buffer for irreversible actions. Prototype, not part of the Orb runtime.",
  "type": "module",
  "engines": { "node": ">=22.18" },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "start": "node src/server.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
